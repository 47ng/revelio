language: rust
rust:
  - stable
#   - beta
#   - nightly
# matrix:
#   allow_failures:
#     - rust: nightly
#   fast_finish: true

script:
  - cargo test  --all --verbose --test-threads=1
  - cargo build --all --verbose --release
  - cargo run niffler --release

cache: cargo
